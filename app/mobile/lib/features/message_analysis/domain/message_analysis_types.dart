enum RiskLevel {
  low,
  medium,
  high,
}

enum RecommendedAction {
  wait,
  doNotReply,
  replyWithBoundary,
  useSos,
  writeUnsentLetter,
}

enum AnalysisConfidence {
  low,
  medium,
  high,
}
